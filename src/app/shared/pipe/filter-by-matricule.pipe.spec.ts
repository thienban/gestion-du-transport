import { FilterByMatriculePipe } from './filter-by-matricule.pipe';

describe('FilterByMatriculePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMatriculePipe();
    expect(pipe).toBeTruthy();
  });
});
