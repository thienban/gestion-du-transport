import { FilterByAdresseArPipe } from './filter-by-adresse-ar.pipe';

describe('FilterByAdresseArPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByAdresseArPipe();
    expect(pipe).toBeTruthy();
  });
});
