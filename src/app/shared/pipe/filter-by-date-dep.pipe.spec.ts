import { FilterByDateDepPipe } from './filter-by-date-dep.pipe';

describe('FilterByDateDepPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDateDepPipe();
    expect(pipe).toBeTruthy();
  });
});
