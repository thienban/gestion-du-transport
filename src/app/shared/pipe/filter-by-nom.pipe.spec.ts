import { FilterByNomPipe } from './filter-by-nom.pipe';

describe('FilterByNomPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNomPipe();
    expect(pipe).toBeTruthy();
  });
});
