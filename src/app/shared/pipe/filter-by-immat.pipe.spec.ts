import { FilterByImmatPipe } from './filter-by-immat.pipe';

describe('FilterByImmatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByImmatPipe();
    expect(pipe).toBeTruthy();
  });
});
