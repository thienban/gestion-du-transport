import { FilterByMarquePipe } from './filter-by-marque.pipe';

describe('FilterByMarquePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMarquePipe();
    expect(pipe).toBeTruthy();
  });
});
