import { FilterByAdresseDepPipe } from './filter-by-adresse-dep.pipe';

describe('FilterByAdresseDepPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByAdresseDepPipe();
    expect(pipe).toBeTruthy();
  });
});
