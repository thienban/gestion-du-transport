import { FilterByPrenomPipe } from './filter-by-prenom.pipe';

describe('FilterByPrenomPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPrenomPipe();
    expect(pipe).toBeTruthy();
  });
});
